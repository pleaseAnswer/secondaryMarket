宜州劳动力采集项目：
原先开发是根据原有的项目框架去做项目开发，在开发过程中也会处理好采集表单的一些用户交互，也处理了一些测试提出的之前有同事没解决的一些用户体验上的问题：ios端小程序操作时存在键盘遮挡选择器组件的问题，做了细致的观察之后找到了导致这种情况发生的原因，并使用了小程序的api去做处理。
做中台改造版本的时候，做脚手架升级，对请求方法、路由拦截的封装，提高项目的可维护性；

织金项目：
主要负责开发的业务也是信息采集，在小额贷采集需求中也实现了多照片上传、预览、删除的一个功能，最终实现的页面风格也是比较贴近劳动力采集的；大屏就是去分析了地图的数据结构然后实现了地图下钻的功能，同时刷新页面数据，就是当地图从市级跳到区县级的同时，大屏数据也同步为区县级的数据。

宜州劳动力采集项目是由我负责前端开发的，需要采集五十多个字段，包括处理输入限制以及修改时的字段回显等一些用户交互